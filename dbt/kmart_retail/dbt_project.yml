name: kmart_retail
version: 1.0.0
config-version: 2
profile: kmart_retail

dbt-cloud:
  project-id: 70506183158510

model-paths: [models]
test-paths: [tests]
macro-paths: [macros]
seed-paths: [seeds]
clean-targets: [target, dbt_packages]

models:
  kmart_retail:
    +materialized: view
    staging:
      +schema: silver
      +tags: [stage]
    intermediate:
      +schema: silver
      +materialized: table
      +tags: [intermediate]
    marts:
      +schema: gold
      +materialized: table
      +tags: [gold]
